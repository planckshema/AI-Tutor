kind = "web"
previewPath = "/"
title = "AI Chat"
version = "1.0.0"
id = "artifacts/chat-ui"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24918

[services.development]
run = "pnpm --filter @workspace/chat-ui run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/chat-ui", "run", "build" ]
serve = "static"
publicDir = "artifacts/chat-ui/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24918"
BASE_PATH = "/"
